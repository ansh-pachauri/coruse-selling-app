const JWT_ADMIN_SECRET ="adminPass";
const JWT_USER_SECRET ="userPass";

//you should have to remove the circula dependencied, because espicially in js they are hard to debug

module.exports= {
    JWT_ADMIN_SECRET,
    JWT_USER_SECRET
}